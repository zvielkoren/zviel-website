auto-install-peers=true
verify-store-integrity=false
# .npmrc
# Disables the strict timing check for registry metadata
minimum-release-age-ignore-missing-time=true

# Ensures peer dependencies don't halt your build
auto-install-peers=true